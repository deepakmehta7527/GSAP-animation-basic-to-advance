// Initialize a new Locomotive Scroll instance to enable smooth scrolling
const scroll = new LocomotiveScroll({
  // Specify the element that Locomotive Scroll should apply to
  el: document.querySelector('#main'),  // The element with the ID "main" (the wrapper for all content)

  // Enable smooth scrolling on the page
  smooth: true  // This option ensures that the scrolling behavior is smooth rather than the default instant scroll
});
